## Packages
framer-motion | For smooth layout transitions and entry animations
clsx | For conditional class merging (utility)
tailwind-merge | For safe tailwind class merging (utility)

## Notes
The backend provides /api/currencies for exchange rates.
Calculation logic happens on the backend via /api/calculate.
Dark mode is the default and only theme for this minimalist design.
